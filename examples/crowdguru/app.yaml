application: expert-connect #google.com:expert
version: 2
runtime: python
api_version: 1

handlers:
- url: /static
  static_dir: static
- url: /.*
  script: guru.py

inbound_services:
- xmpp_message
- xmpp_presence
- xmpp_subscribe

